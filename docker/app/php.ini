; Minimal production-friendly defaults (override per env if needed)
expose_php = Off
memory_limit = 256M
upload_max_filesize = 25M
post_max_size = 25M
max_execution_time = 60

; OPcache (safe defaults)
opcache.enable=1
opcache.validate_timestamps=1
opcache.revalidate_freq=2
